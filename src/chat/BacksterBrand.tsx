import { BacksterIcon } from "./BacksterIcon";
import { EntitySourceBrand } from "./EntitySourceBrand";

export function BacksterBrand() {
  return (
    <EntitySourceBrand
      icon={<BacksterIcon size={14} />}
      label="Backster"
    />
  );
}
