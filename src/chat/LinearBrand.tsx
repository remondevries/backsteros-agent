import { EntitySourceBrand } from "./EntitySourceBrand";
import { LinearIcon } from "./LinearIcon";

export function LinearBrand() {
  return (
    <EntitySourceBrand
      icon={<LinearIcon size={14} />}
      label="Linear"
    />
  );
}
